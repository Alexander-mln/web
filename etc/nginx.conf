server  {
  listen        80;
  server_name   default_server;
  location /uploads/ {
    root      /home/box/web/uploads
  }
  location ~* ^.+\/.+.\.+$ {
    root       /home/box/web/public
  }
  location /uploads/ {
    return     404
  }
}